name: rdp


on:
  workflow_dispatch:
    inputs:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: test python
      run: |
        cd ~/
        cat << EOF >> rdp.txt
        ${{ secrets.URL_CONFIG }}
        EOF
        cat rdp.txt
        wget https://raw.githubusercontent.com/crypt-bphc/Test/main/rdp.sh
        chmod +x rdp.sh
        bash rdp.sh
        CRP=$(cat ~/rdp.txt)
        echo "$CRP"
        su - Arihant -c """$CRP"""
        pecho "Done !"
        sudo apt-get upgrade
